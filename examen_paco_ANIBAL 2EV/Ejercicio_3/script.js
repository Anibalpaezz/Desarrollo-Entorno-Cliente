function parar() {

}


/* document.addEventListener("DOMContentLoaded", function() {
    const loaderContainer = document.querySelector('.loader-container');
    const loaderBar = document.querySelector('.loader-bar');

    loaderContainer.style.width = '500px';
    loaderContainer.style.height = '5px';
    loaderContainer.style.overflow = 'hidden';

    loaderBar.style.height = '100%';
    loaderBar.style.width = '0';
    loaderBar.style.backgroundColor = '#3498db';
    loaderBar.style.animation = 'progress 10s steps(10) infinite';

    const keyframes = [
        { width: '0' },
        { width: '0' },
        { width: '500px' },
        { width: '500px' },
        { width: '0' },
    ];

    loaderBar.animate(keyframes, {
        duration: 10000,
        iterations: Infinity,
    });
}); */


